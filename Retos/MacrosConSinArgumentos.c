/*===========================*
 * El asunto es argumentable *
 *===========================*/

#include <stdio.h>

/*
 * Indique la salida del programa
 */

#define Cuadrado (valor) ((valor) * (valor))

int main(void)
{
    printf("%d\n", Cuadrado(10));
}
