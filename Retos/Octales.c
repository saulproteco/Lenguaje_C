/*============================*
 * Error de compilación ninja *
 *============================*/

#include <stdio.h>

/*
 * El siguiente código no compila, encuentre la razón
 */

int main(void)
{
    int siete = 07;
    int ocho = 08;

    printf("El valor de 'siete' es: %d\n", siete);
    printf("El valor de 'ocho'  es: %d\n", ocho);
}
