/*=====================*
 * Azúcar de funciones *
 *=====================*/

#include <stdio.h>

/*
 * Que imprime el código y por qué
 */

int main(void)
{
    if (*printf == printf && printf == &printf) 
        puts("Uno");
    else
        puts("dos");
}
